services:
  backend:
    build: ./backend
    container_name: backend_container

    ports:  
      - "4000:4000"

    volumes:
      - ./backend:/backend
      - /backend/node_modules

    env_file:
      - ./backend/.env
        
    # Keep the container running and allow interactive terminal in development
    stdin_open: true
    tty: true